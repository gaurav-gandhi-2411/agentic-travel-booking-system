call_budget:
  flight_calls_max: 150
  hotel_calls_max: 100
  llm_calls_max: 20

window_search:
  horizon_days: 30
  window_size_days: 7
  max_windows: 10

destination_cities:
  CDG: Paris
  NRT: Tokyo
  DPS: Bali
